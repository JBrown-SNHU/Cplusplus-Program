#ifndef OTHERFUNCTIONS_H
#define OTHERFUNCTIONS_H
#include <string>

using namespace std;

string nCharString(size_t n, char c);   // Creates a border for design
string isYesOrNo(string response);      // Used for yes or no response 
string isQuit(string response);         // used for terminating the main

#endif
